This folder holds offline NuGet packages.

If this folder is empty, run setup.ps1 (Windows) or setup.sh (Linux/Mac)
from the project root ONCE while you have internet — it downloads everything here.

After that, the project builds with no internet required.
